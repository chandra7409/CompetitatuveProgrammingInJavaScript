function TripletSum(n) {
    arr.sort((a, b) => (a - b));
    for (let i = 0; i < n; i++) {
        let y = arr[i];
        let negative = -1 * arr[i];
    }
}


let arr = [-1, 0, 1, 2, -1, -4];
console.log(TripletSum(arr));